# 课外课记录功能开发计划

## 1. 目标与范围

在现有 `child-rewards` 网站中增加课外课记录功能：

- 首页增加“多乐的上课记录”入口，进入 `/classes` 页面。
- 页面保持现有粉色、可爱、三丽鸥灵感的视觉语言，不迁移框架。
- 布局参考 `workouts.malinkang.com` 的信息层级：左侧宝宝头像与课程概览，右侧月历，下方年度热力图，再下方为上课记录列表。
- 支持画画、英语、体能、跳舞、游泳及后续新增课程。
- 记录可包含图片或视频，点击后使用统一媒体查看器放大图片或播放视频。
- 课程配置、上课记录和媒体文件以 Notion 为数据源。
- 页面和数据涉及孩子隐私，必须复用家长 PIN 的设备授权机制，不向未授权访问者返回记录或媒体地址。

实施默认采用：Notion Files + Worker 授权代理、网页新增记录、手机拍摄和相册双入口。

## 2. 参考实现结论

参考源码：`/Users/malinkang/Documents/Codex/2026-05-20/mtbi/workouts_page_mk`。

采用的结构：

1. 顶部双栏概览。
2. 可切换月份的日历。
3. 可切换年份的年度热力图。
4. 按时间倒序排列、可筛选的记录列表。
5. 点击日历或热力图日期联动筛选记录。

不采用的部分：

- 不引入 React、地图、轨迹、距离统计或深浅色主题。
- 不复制跑步站点的组件和样式，使用当前原生 HTML/CSS/JavaScript 架构重新实现。
- 不把 Notion 数据转换成静态 JSON，保持 Worker 实时读取。

## 3. Notion 数据模型

### 3.1 `孩子资料` 数据库

只保留一条有效记录，包含 `姓名`（Title）、`头像`（Files）、`生日`（Date）和 `启用`（Checkbox）。头像通过 Worker 授权代理读取，不作为公开静态资源部署。

### 3.2 `课外课程` 数据库

每种课程一行，负责课程展示配置和默认信息。

| 属性 | 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `课程` | Title | 是 | 课程名称，例如画画、英语、体能、跳舞、游泳 |
| 页面 Icon | Notion icon | 否 | 前端课程图标，支持 emoji 或 Notion 图片 |
| `颜色` | Select | 是 | 课程主题色，如粉色、蓝色、黄色、薄荷绿、紫色 |
| `老师` | Rich text | 否 | 默认老师名称 |
| `地点` | Rich text | 否 | 默认上课地点 |
| `默认时长` | Number | 否 | 默认分钟数 |
| `启用` | Checkbox | 是 | 只展示启用课程 |
| `排序` | Number | 是 | 课程筛选器及概览中的顺序 |

建议初始数据：

| 课程 | Icon | 颜色 |
| --- | --- | --- |
| 画画 | 🎨 | 粉色 |
| 英语 | 📚 | 蓝色 |
| 体能 | 🏃 | 黄色 |
| 跳舞 | 🩰 | 紫色 |
| 游泳 | 🏊 | 薄荷绿 |

### 3.3 `上课记录` 数据库

每次实际或计划中的课程一行，是日历、热力图和记录列表的唯一数据源。

| 属性 | 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `记录` | Title | 是 | 默认格式：`课程名 - YYYY-MM-DD` |
| `课程` | Relation | 是 | 关联 `课外课程` |
| `上课时间` | Date | 是 | 带开始时间，可选结束时间；统一按北京时间展示 |
| `状态` | Status | 是 | `计划中`、`已完成`、`请假`、`取消`、`试听` |
| `时长` | Number | 否 | 实际上课分钟数 |
| `上课内容` | Rich text | 否 | 本节课学习或练习内容 |
| `课堂表现` | Select | 否 | `很开心`、`认真`、`有进步`、`需要鼓励` |
| `老师点评` | Rich text | 否 | 老师反馈或家长观察 |
| `地点` | Rich text | 否 | 当次上课地点；为空时前端可回退课程默认地点 |
| `媒体` | Files | 否 | 课堂照片或视频，支持多文件 |

数据规则：

- 热力图只统计 `已完成` 和 `试听`，颜色深度表示当天上课次数，范围为 0、1、2、3+。
- 月历显示所有状态；已完成使用课程色圆点，计划中使用空心圆，请假/取消使用低饱和标记。
- 同一天多节课全部保留，不合并原始记录。
- 不在 Notion 中冗余保存年份、月份或热力图数值，均由 Worker/前端从 `上课时间` 派生。
- Notion 属性名是 API 契约，创建后同步写入 `README.md` 和 `AGENTS.md`。

## 4. 页面与交互设计

### 4.1 全站导航

- 在首页和后续数据页面顶部增加统一导航栏，作为整个“多乐成长空间”的页面入口。
- 首期导航包含“星星屋”和“上课记录”；后续可配置增加阅读、身高体重、作品集等页面。
- 当前页面使用清晰的选中状态，导航项使用图标加短标题，不使用大型营销式卡片。
- 桌面端横向展示品牌、页面导航、设备锁定和声音按钮；手机端保留品牌与当前页，其他入口折叠进菜单。
- 导航数据在前端集中配置，页面之间复用同一结构和样式，避免每增加页面都复制导航代码。
- 点击“上课记录”时先检查设备授权；未授权则弹出家长 PIN，验证成功后进入 `/classes`。
- 未授权首页不得预取课程记录、头像或媒体信息。
- 导航栏支持键盘操作、焦点状态和 `aria-current="page"`，手机菜单打开后锁定背景滚动。

### 4.2 `/classes` 页面

#### 顶部导航

- 复用首页全站导航，保持品牌、路由和授权状态一致。
- 页面工具区单独放年份选择、刷新和新增记录按钮，避免把页面操作混入全站导航。
- 桌面保持紧凑单行，手机端允许自然换行，不产生横向滚动。

#### 第一屏双栏

- 左侧“头像卡”：宝宝圆形头像、年度已完成课程数、上课天数、最常参加课程、连续上课周数。
- 右侧“月历卡”：月份前后切换、今天定位、课程色图例、每日课程圆点。
- 点击日期后，滚动并筛选到当天记录；再次点击清除日期筛选。
- 手机端头像卡在上、日历在下。

#### 年度热力图

- 53 列周视图，星期纵轴、月份横轴，使用粉色到莓红色的四级色阶。
- 小屏幕使用可横向滚动的稳定单元格尺寸，不压缩到不可点击。
- 点击日期筛选记录；悬停或轻触显示日期、课程名称和次数。
- 顶部显示年度上课次数、活跃天数和最忙月份。

#### 上课记录

- 默认按 `上课时间` 倒序。
- 支持课程、月份、状态筛选，筛选器采用可横向滚动的彩色图标标签。
- 每条记录展示课程 icon、名称、日期时间、时长、状态、内容摘要和媒体缩略图。
- 多媒体记录展示最多 3 张缩略图及剩余数量。
- 点击图片打开全屏查看器；点击视频打开播放器，支持系统原生播放控制。
- 空状态、加载状态、失败重试和无媒体状态都提供明确反馈。

### 4.3 新增记录

- 页面提供家长可见的“新增上课记录”一级按钮：桌面端位于页面右上角，手机端为底部固定操作按钮。
- 点击后打开全屏抽屉式表单，关闭时保留尚未提交的文字内容，避免误触丢失。
- 表单支持课程、时间、状态、时长、内容、表现、点评、地点和最多 5 个图片/视频。
- 手机文件选择支持直接调用相机拍照或录像，也支持从相册选择已有媒体。
- 上传限制沿用现有兑换媒体规则：图片或视频、单文件不超过 20 MB。
- 提交时由 Worker 上传至 Notion，浏览器不直接接触 Notion Token。
- 提交按钮防重复点击，成功后刷新日历、热力图和列表。
- 新增记录属于家长操作：设备必须已授权，并且每次提交都重新输入家长 PIN。

宝宝头像由用户上传到 Notion `孩子资料`.`头像`，浏览器只访问 `/api/class-avatar`，不能获取原始 Notion Files URL。尚未上传头像时显示粉色蝴蝶结占位图。

## 5. Worker API 设计

新增环境变量：

- `NOTION_COURSES_DATABASE_ID`
- `NOTION_CLASS_RECORDS_DATABASE_ID`
- `NOTION_CHILD_PROFILE_DATABASE_ID`
- `NOTION_CHILD_PROFILE_PAGE_ID`

继续使用现有 Secrets：

- `NOTION_TOKEN`
- `AUTH_PIN`
- `AUTH_SECRET`

新增接口：

| Method | Path | 权限 | 用途 |
| --- | --- | --- | --- |
| `GET` | `/api/classes?year=YYYY` | 已授权设备 | 获取课程、指定年份记录和统计数据 |
| `POST` | `/api/classes` | 已授权设备 + 当次家长 PIN | 新增上课记录及媒体 |
| `GET` | `/api/class-avatar` | 已授权设备 | 代理孩子头像 |
| `GET` | `/api/class-media/:recordId/:index` | 已授权设备 | 代理课堂图片或视频 |

接口约束：

- `year` 只接受合理四位年份，按北京时间构造 Notion 日期范围过滤。
- 查询课程时只取 `启用 = true`，按 `排序` 升序。
- 查询记录时只返回指定年份，按 `上课时间` 倒序，处理 Notion 分页。
- 页面 ID、Relation 来源数据库、状态、时长和文件类型都在 Worker 再验证。
- 不向浏览器返回 Notion 媒体 URL；代理前验证设备 Cookie、页面数据库归属和媒体索引，并设置 `Cache-Control: private, no-store`。
- 视频代理转发 `Range`，保留 `206`、`Accept-Ranges` 和 `Content-Range`。
- 新增记录继续执行同源校验、签名 Cookie 校验、2 秒写入冷却和 PIN 错误限速。
- 未授权返回 `401 AUTH_REQUIRED`，前端转入现有家长解锁流程。
- 不在日志或文档中输出 Notion 签名媒体 URL、PIN 或 Cookie。

## 6. 文件改动规划

预计修改：

- `public/index.html`：首页全站导航。
- `public/app.js`：导航配置、授权状态和页面跳转。
- `public/styles.css`：桌面导航与手机折叠菜单样式。
- `public/shared-nav.js`、`public/shared-nav.css`：跨页面复用的导航配置、结构和响应式样式。
- `src/index.js`：课程查询、新增记录、媒体上传和受保护路由。
- `wrangler.toml`：两个非敏感数据库 ID。
- `README.md`：数据模型、接口、环境变量和部署说明。
- `AGENTS.md`：课程业务规则、隐私边界和验证要求。

预计新增：

- `public/classes.html`：上课记录页结构。
- `public/classes.js`：日历、热力图、筛选、记录和新增表单逻辑。
- `public/classes.css`：粉色课程页视觉和响应式布局。
- 头像由 Notion Files 保存并通过 Worker 授权代理，不增加公开头像资源。

Worker 对 `/classes` 返回 `classes.html`，避免 URL 暴露 `.html`。

## 7. 分阶段实施顺序

### 阶段 A：Notion 建模

1. 在现有 Notion 项目页面下创建 `课外课程` 和 `上课记录`。
2. 创建全部属性、Relation 和初始课程数据。
3. 确认 Integration 对两张数据库都有访问权限。
4. 将数据库 ID 写入 `wrangler.toml`，Token 继续只保存在 Secret。

### 阶段 B：后端与安全

1. 增加受保护的课程读取接口。
2. 增加记录创建和媒体上传接口。
3. 完成字段映射、分页、北京时间范围、参数校验和错误码。
4. 用无写入请求验证未授权访问被拒绝。

### 阶段 C：课程页面

1. 建立页面骨架和顶部导航。
2. 完成头像概览卡与月历。
3. 完成年度热力图及日期联动。
4. 完成筛选、记录列表和媒体查看器。
5. 完成新增记录表单和上传反馈。

### 阶段 D：首页集成

1. 建立可扩展的全站导航配置和通用样式。
2. 增加“星星屋”和“上课记录”导航项，并实现当前页状态。
3. 接入授权检查、桌面导航和手机折叠菜单。
4. 保证首页现有任务、礼物、声音和 PIN 流程不回归。

### 阶段 E：验证与发布

1. 运行 JS 语法检查、`git diff --check` 和 Wrangler dry-run。
2. 使用临时课程记录测试新增、媒体上传、日历和热力图。
3. 只按精确页面 ID 归档临时记录，并确认无测试数据残留。
4. 使用桌面和 390px 手机视口检查布局、溢出、弹窗和媒体播放。
5. 验证未授权用户无法读取 `/api/classes` 或媒体 URL。
6. 部署 Cloudflare Worker，验证正式域名和缓存版本。
7. 更新文档，提交并推送 `main`。

## 8. 验收标准

- 首页导航清晰但不破坏当前星星屋主流程。
- 全站导航在桌面和手机上均可用，新增页面时无需重写导航结构。
- 未授权访问者不能看到孩子头像、课程记录、照片或视频。
- 授权后可按年浏览，月历、热力图和列表数据一致。
- 同一天多节课程能正确显示和统计。
- 课程筛选、月份筛选、状态筛选可组合使用。
- 图片可放大，视频可播放，关闭查看器后恢复原滚动位置。
- 新增记录成功写入 Notion，Relation、时间、状态和媒体字段正确。
- 桌面与手机端都能从明显的“新增上课记录”按钮完成录入，手机可直接拍照或录像。
- 手机和桌面均无文本遮挡、横向溢出或不可点击控件。
- 现有星星获得、余额 Rollup、礼物兑换、PIN 和语音功能无回归。
- Notion Token、家长 PIN、签名密钥和私有媒体 URL不进入 Git。

## 9. 开发前需要的素材与确认

开发开始时需要：

1. 一张宝宝头像原图。
2. 确认公开 GitHub 仓库中是否允许提交头像；默认按“不允许”处理。
3. 确认是否需要在网页内新增记录；本计划默认支持，Notion 后台仍可直接编辑。
4. 确认初始课程名称、老师、地点和默认时长；缺失项可以先留空。
