# 首页获得星星热力图

- 日期：2026-09-21
- 状态：pending

## 目标与范围

在首页「今天摘哪颗星？」任务卡片下方增加独立年度热力图卡片，延续现有粉色视觉和原生 HTML/CSS/JavaScript 实现。

- 使用 `/api/rewards` 返回的完整账本，仅统计 `type = earn` 且星星数为正的记录。
- 按北京时间将获得记录归入日期；颜色表示当日获得星星数，同一天的多条记录全部计入。
- 默认展示当前年份，支持切换有获得记录的年份；正确处理闰年和跨年周。
- 提示只展示当天各条非空 `说明`，保留同日多条说明，不额外加入日期、标题或星星数量。支持鼠标、键盘和手机点按。
- 保留任务卡片、概览、完整成长册和兑换行为。新增热力图只读，不修改 Notion 数据或余额计算。
- 保持横向滚动在热力图内部，覆盖加载、失败、空记录状态。
- 完成测试后提交实现并 push `main`；此次范围不包含生产部署。

## 实施步骤

1. 添加日历/北京时间归组辅助模块及必要的边界测试。
2. 在任务卡片下方添加热力图，接入加载、刷新、任务完成后的重新渲染，完善提示交互和响应式样式。
3. 同步 README、AGENTS 中的热力图行为说明，更新静态资源版本。
4. 完成验证，更新本计划状态，创建独立完成提交并推送。

## 验收与验证

- 仅获得记录影响热力图；同日两条获得记录及一条兑换记录得到两条说明和正确获得数量。
- 纯日期、北京时间跨日/跨年、闰年 2 月 29 日、需要 54 周列的年份均正确。
- 提示安全展示说明中的 HTML 字符；空说明不回退到标题。悬停、聚焦、点按可见，Escape/离开/外部点按可关闭。
- 桌面和手机布局无页面横向溢出，热力图可横向滚动；加载/错误/空状态、年份切换和刷新正常。
- 运行 `node --check src/index.js`、`node --check public/app.js`、`git diff --check`、`npm run check` 及相关 Node 测试。
- 浏览器使用模拟 API 数据验证交互，避免真实 Notion 写入。

## 完成记录

待实现与验证。
